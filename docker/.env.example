# hermes-dispatch Docker environment.
# Copy to .env and edit:  cp .env.example .env
# .env is gitignored. Never commit real keys.

# ─── Required ────────────────────────────────────────────────────────────────
# The master key the dispatch server uses to authenticate to LiteLLM. Any
# non-empty string; change it from the default.
LITELLM_MASTER_KEY=sk-local-change-me

# ─── Backend posture, pick ONE ───────────────────────────────────────────────
# (a) CLOUD (default below): set the provider keys you actually use. Unused ones
#     can stay blank. LiteLLM reads these automatically.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# (b) LOCAL OLLAMA on the host: set OLLAMA_BASE and switch the MODEL_* values
#     below to ollama_chat/<tag>. The container reaches the host via
#     host.docker.internal. Leave blank for cloud.
OLLAMA_BASE=
# OLLAMA_BASE=http://host.docker.internal:11434

# ─── Model tiers (required) ──────────────────────────────────────────────────
# The capability/cost spine. Every agent resolves to one of these three.
# Defaults target cloud (see examples/models-cloud.yaml). For local Ollama, set
# OLLAMA_BASE above and use ollama_chat/<tag> values.
MODEL_FAST=openai/gpt-4o-mini
MODEL_BALANCED=openai/gpt-4o-mini
MODEL_MAX=anthropic/claude-3-5-sonnet-latest

# ─── Task roles (optional) ───────────────────────────────────────────────────
# Leave blank to INHERIT the default tier shown. Set one only when a specialized
# model beats the tier for that job.
MODEL_STRUCTURED=    # inherits MODEL_FAST
MODEL_CODE=          # inherits MODEL_BALANCED
MODEL_WRITING=       # inherits MODEL_BALANCED
MODEL_REASONING=     # inherits MODEL_MAX

# ─── Optional ────────────────────────────────────────────────────────────────
# Obsidian save-to-vault (leave blank to disable). Must be a path reachable
# INSIDE the dispatch container. Mount your vault as a volume if you enable it.
OBSIDIAN_VAULT=
